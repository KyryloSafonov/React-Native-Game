import {StyleSheet} from 'react-native';

const styleGameOverScreen = StyleSheet.create({
  screen: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  textItem: {
    fontSize: 25,
  },
});

export default styleGameOverScreen;
